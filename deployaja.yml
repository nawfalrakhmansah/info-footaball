name: football
version: 1.0.0
description: Info Football App

container:
  image: ghcr.io/nawfalrakhmansah/info-footaball:latest
  port: 80

resources:
  cpu: 500m
  memory: 512Mi
  replicas: 1

env:
  - name: NODE_ENV
    value: production

domain: info-football.deployaja.id
